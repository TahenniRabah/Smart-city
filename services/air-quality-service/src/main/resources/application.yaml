server:
  port: 8080

spring:
  application:
    name: air-quality-service

  kafka:
    bootstrap-servers: localhost:9092

urbanhub:
  kafka:
    topics:
      measurements-validated: measurements.validated
      air-quality-alert-detected: air-quality.alert.detected


springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    operations-sorter: method
    tags-sorter: alpha
